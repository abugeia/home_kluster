# CRDs Gateway API (canal standard) — installées ici et non plus par le chart Traefik.
#
# Historique : le chart traefik <= 39.x livrait `crds/gateway-standard-install.yaml`
# (Gateway API v1.4.0 standard). La release 40.0.0 a arrêté de les fournir. Comme
# l'Application `traefik` tourne en `prune: true`, l'upgrade vers 41.x supprimerait
# ces CRDs : cette Application doit donc exister et avoir pris le tracking ArgoCD
# AVANT le bump du chart.
#
# Version v1.5.1 imposée par Traefik v3.7.x, et NON alignée sur la lib
# gateway-api que son binaire embarque (v1.4.0) : le provider watche TLSRoute
# inconditionnellement (pkg/provider/kubernetes/gateway/client.go, l'informer est
# enregistré hors du bloc `if c.experimentalChannel`) et la liste via le package
# `v1` (`ListTLSRoutes() []*gatev1.TLSRoute`). Or TLSRoute n'est servie en `v1`
# qu'à partir de Gateway API v1.5.0 ; jusqu'en v1.4.0 elle n'existe qu'en
# v1alpha2/v1alpha3, y compris dans le canal experimental.
#
# Conséquence de l'absence : WatchAll appelle WaitForCacheSync sur tous les
# informers au démarrage, celui de TLSRoute ne se synchronise jamais, l'event loop
# ne démarre pas et le provider reste inerte — GatewayClass et Gateway bloqués sur
# "Waiting for controller" (Accepted=Unknown), HTTPRoute sans statut. Le tout sans
# qu'aucun TLSRoute ne soit utilisé. Traefik v3.6 tolérait cette absence, v3.7 non.
#
# Canal standard suffisant : TCPRoute, elle, n'est watchée que si
# experimentalChannel est activé, ce qui n'est pas le cas ici.
#
# ServerSideApply : obligatoire, pas une précaution. La CRD httproutes pèse ~345 Ko,
# soit 131 % de la limite de 262144 octets de l'annotation last-applied-configuration
# -> un apply client-side échoue sur `metadata.annotations: Too long`.
#
# Pas de `finalizers: resources-finalizer.argocd.argoproj.io` ici, volontairement :
# sur une App de CRDs, supprimer l'App entraînerait la suppression en cascade de
# TOUS les objets Gateway/HTTPRoute du cluster.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gateway-api-crds
  namespace: argocd
  annotations:
    # Wave négative : root-app synchronise cette Application avant celle de Traefik,
    # pour que le tracking ArgoCD des CRDs soit transféré ici avant que le chart
    # cesse de les déclarer (sinon son `prune` les emporte).
    argocd.argoproj.io/sync-wave: "-1"
spec:
  project: default
  source:
    repoURL: https://github.com/kubernetes-sigs/gateway-api
    targetRevision: v1.5.1
    path: config/crd/standard
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  syncPolicy:
    automated:
      # prune désactivé : on ne laisse pas ArgoCD supprimer des CRDs cluster-scoped.
      prune: false
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
