apiVersion: v1
kind: Service
metadata:
  name: filebrowser
  namespace: media
  labels:
    app: filebrowser
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: filebrowser
  type: ClusterIP
