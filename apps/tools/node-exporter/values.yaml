# Node Exporter configuration for Home Kluster

# Annotations pour que vmagent scrape automatiquement le Pod
podAnnotations:
  prometheus.io/scrape: "true"
  prometheus.io/port: "9100"

# On disable RBAC PSP car déprécié
rbac:
  pspEnabled: false

# Optimisations ressources
resources:
  requests:
    cpu: 10m
    memory: 16Mi
  limits:
    cpu: 100m
    memory: 64Mi

# Désactiver les collecteurs trop lourds ou inutiles
extraArgs:
  - --collector.filesystem.mount-points-exclude=^/(dev|proc|sys|var/lib/docker/.+|var/lib/kubelet/.+)($|/)
  - --collector.filesystem.fs-types-exclude=^(autofs|binfmt_misc|bpf|cgroup2?|configfs|debugfs|devpts|devtmpfs|fusectl|hugetlbfs|iso9660|mqueue|nsfs|overlay|proc|procfs|pstore|rpc_pipefs|securityfs|selinuxfs|squashfs|sysfs|tracefs)$
