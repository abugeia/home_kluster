apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kms-var
  namespace: tools
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 50Mi
