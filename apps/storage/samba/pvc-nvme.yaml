apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-samba-nvme
  namespace: storage
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi-nvme
  volumeName: pv-nvme-root
  resources:
    requests:
      storage: 1Ti
