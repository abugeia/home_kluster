# Reflector Helm Values
# On reste sur les valeurs par défaut pour le moment
replicaCount: 1
configuration:
  # On peut restreindre si besoin, mais par défaut il surveille tout
  watcher:
    namespaces: []

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 20m
    memory: 64Mi
