# Configuration officielle du chart Traefik (Simplifiée via cert-manager)
# Référence: https://github.com/traefik/traefik-helm-chart

deployment:
  podAnnotations:
    prometheus.io/port: "8082"
    prometheus.io/scrape: "true"
  priorityClassName: "system-cluster-critical"

tolerations:
  - key: "CriticalAddonsOnly"
    operator: "Exists"
  - key: "node-role.kubernetes.io/control-plane"
    operator: "Exists"
    effect: "NoSchedule"

providers:
  kubernetesCRD:
    enabled: true
    # Permet de référencer des Middlewares situés dans d'autres namespaces (ex: security, tools)
    allowCrossNamespace: true
  kubernetesIngress:
    publishedService:
      enabled: true
    allowEmptyServices: true

logs:
  access:
    enabled: true

# PERSISTANCE : Activée sur /plugins-storage pour stabiliser les plugins (Sablier).
# Évite que les plugins ne soient re-téléchargés (et potentiellement perdus) à chaque redémarrage.
persistence:
  enabled: true
  accessMode: ReadWriteOnce
  size: 1Gi
  path: /plugins-storage

ports:
  web:
    forwardedHeaders:
      insecure: true
  websecure:
    tls:
      enabled: true
    forwardedHeaders:
      insecure: true

service:
  spec:
    externalTrafficPolicy: Local
  annotations:
    metallb.io/load-balancer-ip: 10.0.0.101

experimental:
  plugins:
    sablier:
      # Plugin extrait dans un repo dédié depuis fin 2025 (l'ancien module
      # github.com/sablierapp/sablier est figé en v1.10.x et n'est plus maintenu).
      # v1.3.0 apporte : fix cache page d'attente/redirect loop (#37), fix SSE hang (#36),
      # keep-alive connexions longues (#35), failOpen (#40).
      moduleName: "github.com/sablierapp/sablier-traefik-plugin"
      version: "v1.3.0"
